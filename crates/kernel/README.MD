```sh
sbk --service fs:///etc/switchboard/service.toml --config /etc/switchboard/config.toml
# sbk --service /etc/switchboard/config.toml
```
```
[ConfigService] <=> [Supervisor] <=> [RunningServices]
```

